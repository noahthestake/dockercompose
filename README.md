After code is complete in compose.yml, open terminal.
In the terminal, run "docker compose up" and open Docker Desktop.
On Docker Desktop, open the container that is active.
After the container is up, open the port link.
The port link should open a page showing the equivalent screenshot provided.
